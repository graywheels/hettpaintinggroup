<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Owner Login - Desert Ridge Contracting</title>
    
    <style>
        /* Basic layout to center the form */
        body  {
            font-family: sans-serif;
            display: grid;
            place-items: center;
            min-height: 80vh;
            background-color: #f9f9f9;
        }
        /* hi this s\is \\austin */
        /* The form container */
        .login-container {
            width: 300px;
            padding: 20px;
            border: 1px solid #ccc;
            background-color: #fff;
            text-align: center; /* Center the logo and title */
        }
        
        /* Make form elements stack nicely */
        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        /* Make inputs and buttons take up the full width */
        input[type="text"],
        input[type="password"],
        button {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            box-sizing: border-box; /* Important for 100% width */
        }

        /* The logo image */
        .login-logo {
            width: 200px;
            margin-bottom: 15px;
        }

        /* EJS error message styling */
        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px;
            margin-bottom: 15px;
        }

        /* "Back to Home" link */
        .back-link {
            display: block;
            margin-top: 15px;
            font-size: 0.9em;
        }
    </style>
</head>
<body>

    <main class="login-container">
        
        <img src="/images/desert-ridge-logo.png" alt="Logo" class="login-logo">

        <h3>Owner Login</h3>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        <form action="/login" method="POST">
            
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" required>
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>
            </div>

            <button type="submit">Sign In</button>
        </form>

        <a href="/" class="back-link">&larr; Back to Home</a>
    </main>

</body>
</html>